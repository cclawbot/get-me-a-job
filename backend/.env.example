# Server Configuration
PORT=3001

# Database
DATABASE_URL="file:./dev.db"

# AI Service - Google Gemini CLI
# Requires gemini CLI installed: npm install -g @google-gemini-cli/gemini
# Uses Google Cloud Authentication (GCA)
GOOGLE_GENAI_USE_GCA=true
ENABLE_AI_FEATURES=true
